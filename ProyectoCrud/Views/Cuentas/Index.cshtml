@model List<ProyectoCrud.Models.Cuenta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().SaldoTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().Estado)
            </th>
            <th>
                <label>Socio</label>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoTotal)
            </td>
            <td>
                @if (item.Estado == 1)
                {
                    <label>Activado</label>
                }
                else
                {
                    <label>Desactivado</label>
                }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoSocioNavigation.Cedula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Numero">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Numero">Details</a> |
                @if (item.Estado == 1)
                {
                    <a asp-action="Activar" asp-route-id="@item.Numero">Activar</a>
                }
                else
                {
                    <a asp-action="Desactivar" asp-route-id="@item.Numero">Desactivar</a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
